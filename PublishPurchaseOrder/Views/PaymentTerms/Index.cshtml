@model IEnumerable<PurchaseOrderApp112916.Models.PaymentTerms>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PaymentTermsDescription)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PaymentTermsDescription)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PaymentTermsID }) |
            @Html.ActionLink("Details", "Details", new { id=item.PaymentTermsID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PaymentTermsID })
        </td>
    </tr>
}

</table>
