@model PurchaseOrderApp112916.Models.ShippingMethods

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>ShippingMethods</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.ShippingMethodDescription)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.ShippingMethodDescription)
        </dd>

    </dl>
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.ShippingMethodID }) |
    @Html.ActionLink("Back to List", "Index")
</p>
